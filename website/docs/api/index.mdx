---
sidebar_position: 0
---

# API reference

The API reference, rendered **inside the site** from the TypeScript
source: docstring first, centered on the primitives rather than
alphabetical declaration lists. Everything here is extracted at build
time (`typedoc --json` → curated model → site components), so it cannot
drift from the code — and referenced type names in the type chips carry
**hover cards** with their definition and doc.

## pond-ts (core)

**Temporal keys** — [`Time`](./core/time),
[`TimeRange`](./core/time-range), [`Interval`](./core/interval) — and
[`Event`](./core/event), the key + payload pair.

**Grids** — [`Sequence`](./core/sequence),
[`BoundedSequence`](./core/bounded-sequence).

**Batch series** — [`TimeSeries`](./core/time-series),
[`PartitionedTimeSeries`](./core/partitioned-time-series),
[`ValueSeries`](./core/value-series).

**Live series** — [`LiveSeries`](./core/live-series),
[`LiveView`](./core/live-view),
[`LivePartitionedSeries`](./core/live-partitioned-series),
[`LivePartitionedView`](./core/live-partitioned-view).

**Functions** — [`toTimeRange`, `top`](./core/functions).

## @pond-ts/charts

**Structure** — [`<ChartContainer>`](./charts/chart-container),
[`<ChartRow>`](./charts/chart-row), [`<Layers>`](./charts/layers),
[`<XAxis>`](./charts/x-axis), [`<YAxis>`](./charts/y-axis),
[`<Canvas>`](./charts/canvas), [`<TimeAxis>`](./charts/time-axis),
[`<CategoryAxis>`](./charts/category-axis).

**Draw layers** — [`<LineChart>`](./charts/line-chart),
[`<AreaChart>`](./charts/area-chart),
[`<BandChart>`](./charts/band-chart),
[`<BarChart>`](./charts/bar-chart),
[`<ScatterChart>`](./charts/scatter-chart),
[`<BoxPlot>`](./charts/box-plot),
[`<Candlestick>`](./charts/candlestick).

**Annotations** — [`<Baseline>`](./charts/baseline),
[`<Marker>`](./charts/marker), [`<Region>`](./charts/region),
[`<YAxisIndicator>`](./charts/y-axis-indicator).

**Functions** — [data adapters](./charts/data-adapters),
[theming](./charts/theming),
[scales & live values](./charts/scales-and-live).

## Other packages

`@pond-ts/react`, `@pond-ts/fit`, and `@pond-ts/financial` aren't in
the in-site reference yet — their full generated references remain
available:

- [@pond-ts/react](pathname:///generated-api/react/)
- [@pond-ts/fit](pathname:///generated-api/fit/)
- [@pond-ts/financial](pathname:///generated-api/financial/)
