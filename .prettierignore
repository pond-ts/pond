node_modules
.npm-cache
dist
coverage
package-lock.json

# Docusaurus docs site build output & generated API docs (mirrors website/.gitignore).
# Prettier does not read nested .gitignore files, so these must be listed here for
# the root format scripts to skip them.
website/build
website/.docusaurus
website/.cache-loader
website/static/generated-api
website/static/api
# Typedoc's JSON output and the model built from it, both generated by
# `npm run build` in website/. Absent in a fresh clone, so CI never saw them —
# but anyone who builds the site locally and then runs `npm run verify` got 40
# spurious format failures without these two lines.
website/.api-json
website/src/api-model

# The Pond brand spec is a bundled interactive HTML deliverable from the
# design-system agent, not authored source — left byte-for-byte as delivered.
brand/Pond Brand Spec.html

# The Gallery page template is mostly instructional MDX comment, and prettier
# reformats a multi-line `{/* … */}` into escaped markdown (`\*`, collapsed
# lists) — which shreds it. It's a template, never a built page.
website/docs/charts/gallery/_template.mdx
