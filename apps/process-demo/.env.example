# The composer falls back to an offline keyword matcher without this.
# Set it to run the actual [PND-PROCREG] experiment.
ANTHROPIC_API_KEY=

# Optional overrides.
# PROCESS_DEMO_MODEL=claude-opus-5
# PROCESS_DEMO_NO_FALLBACKS=1
# PORT=8787
